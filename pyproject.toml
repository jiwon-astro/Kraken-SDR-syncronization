[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kraken"
description = "Kraken SDR synchronization package"
version = "0.2.0"
dependencies = []

[tool.setuptools]
packages = ["kraken"]